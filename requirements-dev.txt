# Dev / test dependencies. Baked into the image's `dev` stage (see the
# Dockerfile) so ./test.sh does not reinstall them on every run; CI additionally
# installs them into the *prod* image when the shipped runtime changes, to run
# the suite on the Python that actually ships.
#
# Not in requirements.txt on purpose — none of this belongs in production.
pytest==9.1.1
# Parallel test runs. Safe only because tests/conftest.py derives its TEST_PREFIX
# from PYTEST_XDIST_WORKER — every worker owns its own database rows. Read the
# note there before changing how test users are named.
pytest-xdist==3.8.0
# The linter ./test.sh runs before pytest (#264). It lives here rather than
# being installed per run so the image's `dev` stage bakes it in.
#
# ⚠️ THIS VERSION IS PINNED IN TWO PLACES and they must agree: here, and the
# `version:` input of astral-sh/ruff-action in .github/workflows/ci.yml. The
# action defaults to whatever ruff is newest, so leaving it unpinned reopens the
# exact hole this dependency exists to close — a newer ruff in CI flags what the
# local one accepts, and the local run stops predicting the remote one. The two
# are asserted equal by tests/test_lint_local.py.
ruff==0.16.4
